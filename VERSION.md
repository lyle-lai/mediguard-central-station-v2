# MediGuard CMS - 版本说明书 (Version 3.0 Stable)

**日期:** 2024-05-24
**版本标签:** `v3.0-production-ready`

本文档标记了前端架构重构后的稳定版本。此版本实现了严格的**前后端分离架构**，支持**多科室独立配置**，并建立了生产级的**WebSocket通信协议**。

## 📦 核心功能特性 (Core Features)

### 1. 架构升级：以床位为中心 (Bed-Centric Architecture)
*   **物理床位管理**: 主界面网格由科室的“床位容量”驱动生成，而非仅显示已连接设备。
*   **空床操作**: 支持可视化空床位卡片，点击即可直接办理“患者入科”与“设备绑定”。
*   **灵活排序**: 支持“按床号顺序”与“有人床位优先”两种视图模式切换。

### 2. 严格的 API 协议 (Strict API Protocol)
*   **代码优先 (Code-First)**: 所有内部逻辑、API 交互均使用英文代码 (如 `MONITOR`, `ICU`)，彻底消除中文作为 Key 的隐患。
*   **本地化显示**: 前端通过映射表 (`DeviceTypeDesc`, `DepartmentDesc`) 负责将代码转换为中文界面显示。
*   **ID 引用**: 所有数据修改操作（如配置更新、患者出科）均强制使用 UUID (`patientId`, `deviceId`)。

### 3. 高性能 WebSocket 协议 (Batched Real-time)
*   **载荷瘦身**: 推送消息仅包含 `deviceId` 和物理数据，移除患者姓名、科室等冗余静态信息（由前端缓存映射）。
*   **波形批处理**: 波形数据以 `number[]` 数组形式发送（如 1秒 100个点），大幅降低网络开销并提升渲染流畅度。
*   **稀疏更新**: 参数数据（如心率数值）仅在变化时发送，减少带宽占用。

### 4. 多科室配置隔离 (Department-Specific Config)
*   **独立策略**: 设备显示模板（波形颜色/顺序）和告警阈值策略现在是**与科室绑定**的。ICU 和 普通病房 可以拥有完全不同的配置。
*   **数据结构**: `SystemSettings` 中的配置项升级为 `Record<DepartmentCode, Config>` 映射结构。
*   **模拟器适配**: 模拟引擎 (`iotSimulator`) 能够根据患者所属科室，动态加载对应的规则引擎进行计算。

### 5. 健壮的状态管理 (Robust State Management)
*   **乐观更新与回滚 (Optimistic UI + Revert)**: 设置修改立即生效，但若后端 API 调用失败，界面会自动回滚，确保数据一致性。
*   **防崩溃机制**: 在 `App.tsx` 初始化及组件渲染层增加了严格的空值检查（Optional Chaining），防止因数据未加载完成导致的白屏。
*   **模拟数据对齐**: `MOCK_USERS` 结构已严格对齐新的 `User` 接口，彻底修复了登录时的崩溃问题。

## 🔗 接口规范概览 (API Specification)
详细定义请参考 `api.md`。

*   **认证**: `/auth/login` (返回 Token 及用户授权的科室列表)
*   **配置**: `/config` (获取/更新全科室的容量与模板配置)
*   **实时**: `/ws-mediguard` (WebSocket 端点，订阅 `/topic/vitals`)
*   **历史**: `/patients/{id}/trends` (趋势数据), `/alarms/{id}/snapshot` (报警全息快照)

## 🎨 UI / UX 改进
*   **大屏模式**: 专用的高对比度头部设计，集成超大时钟和报警跑马灯，适合电视墙展示。
*   **标准头部**: 采用胶囊式 (Pill-shaped) 过滤器设计，优化布局防止报警条遮挡操作区。
*   **提示系统**: 集成全局 `Toast` 组件，为所有 API 操作提供明确的成功/失败反馈。
*   **交互优化**: 设置页面的删除/重置操作改为本地状态缓冲，提升操作流畅度。

---
*此版本作为后续开发的基准锚点。*